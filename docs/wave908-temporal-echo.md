# Wave 908 — Temporal Echo

Temporal Echo detects repeated fingerprints in an ordered observation stream.
It reports positions and recurrence gaps. It deliberately does not infer
causation, importance, confidence, or truth from recurrence.

This makes repeated motifs visible to the Observatory while preserving a
strict descriptive boundary.
