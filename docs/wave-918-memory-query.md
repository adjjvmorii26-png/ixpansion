# Wave 918 — Memory Query

Wave 918 adds deterministic multi-token lookup over the Wave 917 memory index.
It returns the union of matching memory IDs and explicitly reports missing
tokens. It does not rank memories or assign confidence.

This remains descriptive retrieval infrastructure.
