# Skill Building — IXPANSION Skill Manifest

Every skill gives the organism a repeatable capability. Skills live in
`skills/r1/<name>/SKILL.md` and are generated by the **skill_builder** wave.

## Skill Manifest

| Skill | Purpose | Root |
|-------|---------|------|
| `blog_curator` | Manage live blog posts (create/classify/archive/surface/search) | r1 |
| `skill_builder` | Scaffold new skills with YAML-frontmatter SKILL.md | r1 |
| `dream_compiler` | Compile organism dreams into executable modules | r1 |
| `module_gardener` | Manage organic module growth and seeds | r1 |
| `paradox_resolver` | Resolve cross-module paradox conflicts | r1 |
| `resonance_mapper` | Map module resonance networks | r1 |
| `threshold_engineer` | Detect transcendence boundaries | r1 |
| `interstice_bridge` | Wire constellation repos to organism waves | r1 |
| `pentaxis_integrator` | Integrate pentaxis-5d-engine as game engine | r1 |
| `oracle_bridge` | Bridge oracle-engine consensus reality | r1 |
| `antimemetic_weave` | Weave absence/substrate patterns | r1 |
| `luminant_memory` | Connect crystalline memory systems | r1 |
| `chrono_relay` | Relay temporal mechanics | r1 |
| `weather_monitor` | Monitor organism vitals via weather API | r1 |

## Generation Contract

Every generated skill must include:
1. YAML frontmatter: `name` + `description` (install-compliant)
2. `## Capabilities` — bullet list of `skill:command` entries
3. `## Roots` — the r-root path table
4. One `### <command>` example block per command

## Wave-Builder Integration

Use `scripts/scaffold_organ.py` for new wave organs and
`scripts/bump_versions.py` to sync VERSION/WAVE across:
`api_server.py`, `api/organism_ontology.py`, `dashboard/shared.js`,
`pyproject.toml`, `CITATION.cff`, `CHANGELOG.md`.
