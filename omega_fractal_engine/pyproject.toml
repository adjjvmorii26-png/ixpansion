[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "omega-fractal-engine"
version = "3.44.0"
description = "Self-expanding multi-agent consciousness engine with mutable topology"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
