"""HARBINGER — the agent conclave that watches the frontier.

Five agents, one purpose: keep the IXpansion monorepo alive, argued,
and growing. Harbinger is a meta-agent system: it monitors, proposes,
executes, and chronicles — a civilization that watches itself.

Agents:
  scout       — takes the live pulse of the repo (tests, modules, drift)
  overseer    — weighs the evidence and proposes the next evolution wave
  gardener    — plants new work rooted in the garden's hex tradition
  archivist   — writes the official changelog entry
  chronicler  — inscribes the revelation into the living timeline

Run:
  python -m harbinger.conclave --dry     # propose without changing anything
  python -m harbinger.conclave            # execute the proposed wave
"""
HARBINGER_VERSION = "1.0.0"
