"""IXpansion Gateway — the public interface to the frontier.

Transforms 360 internal modules into a tiered, rate-limited API with
intent-based routing, usage tracking, and API key auth.
"""
from __future__ import annotations

VERSION = "1.0.0"
