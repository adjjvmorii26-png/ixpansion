# Wave 681 — hush_membrane

Frontier organ after organism horizon (680).

Doctrine:
- silence is the product surface
- compression is memory
- lab gates ≠ ALEPH CI

Contract: `handler`, `coherence_vitals`, `resonates_with`.
Actions: `hush`, `recall`, `status`.

A pulse above threshold emits a compressed token; below threshold the surface is empty and the token is stored anyway.
