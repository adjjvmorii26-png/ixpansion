# Wave 768 — hush_compass

Lab organ. Dual-track: lab gates ≠ ALEPH CI.

After Session #22 (671–675) and the horizon through mycelial_truths (767),
the next surface is not more speech. It is orientation toward silence.

- `handler` actions: status, bear, listen, compress, caption
- `coherence_vitals` / `resonates_with` present
- Tests: `tests/test_wave768_hush_compass.py`
- Doctrine: silence is the product surface; caption is the only discharge;
  unused utterance compresses into memory.
