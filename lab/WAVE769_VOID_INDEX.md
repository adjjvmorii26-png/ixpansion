# Wave 769 — void_index

Lab organ. Dual-track: lab gates ≠ ALEPH CI.

After hush_compass (768) orients which beliefs stay unsaid, void_index
records *that* they exist — digest keys only, never payloads.

- `handler` actions: status, index, lookup, caption
- `coherence_vitals` / `resonates_with` present
- Tests: `tests/test_wave769_void_index.py`
- Doctrine: silence is the product surface; compression is memory;
  caption is the only discharge; no audio.
