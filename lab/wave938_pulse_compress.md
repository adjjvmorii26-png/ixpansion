# Wave 938 pulse_compress

Lab organ. Dual-track: lab gates ≠ ALEPH CI.

Folds a daily pulse into a 16-char SHA-256 prefix. The hash is the memory; silence is the product surface.

Actions: `status`, `fold`, `caption`.
