# Wave 822 — gate_priority

Frontier organ after still_interval (821).

**Contract:** `handler`, `coherence_vitals`, `resonates_with`

**Doctrine:** organism gates > GHAS / full ALEPH CI noise · lab gates ≠ ALEPH CI · silence is the product surface

Organism gates that authorize merge:

- gate
- council-smoke
- boot
- smoke
- graft
- lint

Noise that must not block a green lab organ:

- full `test` matrix
- github-advanced-security / GHAS

`handler({action: rule, checks: {...}})` returns `merge_ok` from lab gates only.
If lab is green and noise is red, token is `merge_over_noise`.

No audio. Caption-only surface.
