# Wave 941 — hush_fold

Frontier organ after residual_chain_cap (940).

Folds the last cap caption into a 16-char hush token. Does not re-bind or re-cap.
Silence is the product surface. Compression is memory. Lab gates ≠ ALEPH CI.

Actions: `status` | `fold` | `caption`
